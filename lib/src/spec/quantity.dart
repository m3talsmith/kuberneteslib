/// Represents a numerical quantity as a double-precision floating-point number.
/// 
/// This type alias is used throughout the application to clearly indicate when
/// a double value specifically represents a quantity (e.g., amount, volume, or count)
/// rather than other numeric values like coordinates or percentages.
typedef Quantity = double;
